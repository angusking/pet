server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/pet?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: ${MAX_FILE_SIZE:20MB}
      max-request-size: ${MAX_REQUEST_SIZE:25MB}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

app:
  jwt:
    secret: ${JWT_SECRET:change-me-to-a-32-byte-secret-key}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:120}
  upload:
    base-dir: ${UPLOAD_BASE_DIR:./uploads}
    public-base: /uploads
  ai:
    session-max-user-messages: ${AI_SESSION_MAX_USER_MESSAGES:5}
    provider: ${AI_PROVIDER:aiservice}
    ai-service:
      base-url: ${AI_SERVICE_BASE_URL:http://localhost:8001}
      model-name: ${AI_SERVICE_MODEL_NAME:aiservice}
