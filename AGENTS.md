# Pet 仓库协作说明

## 仓库范围
- 仓库根目录是 `D:\pet`。
- 这是一个多模块项目，不要把它当成只有 `backend` 的独立工程。
- 主要目录职责如下：
  - `backend`：Spring Boot 后端代码
  - `frontend`：前端应用代码
  - `AIService`：AI 相关服务代码
  - `deploy`：部署文件与导出产物
  - `docs`：项目文档
  - `AIlog`：调试日志目录，不是源码目录
  - `uploads`：运行时上传或输出目录

## 工作规则
- 进行架构判断或分析影响范围时，从仓库根目录视角理解项目。
- 修改时只动任务真正涉及的模块，不要无依据地跨目录扩散改动。
- 不要把 `AIlog` 当成代码模块、文档来源或长期配置目录。
- 优先沿用现有结构和约定，不随意引入新的组织方式。
- 尽量进行比较详细的注释说明，使用中文

## 模块职责

### `backend`
- Java 21 + Spring Boot 后端模块。
- 负责 API、鉴权、业务逻辑、持久化、Flyway 数据库迁移，以及后端侧 AI 集成。
- 典型目录：
  - `src/main/java`：controller、service、repository、entity、config、security
  - `src/main/resources`：应用配置与 Flyway migration
  - `src/test/java`：单元测试和集成风格测试

### `frontend`
- 负责界面、前端路由、接口调用和用户交互流程。
- 后端接口契约变化时，通常需要同步检查前端是否需要调整。

### `AIService`
- 负责后端主模块之外的 AI 相关服务代码。
- 如果修改 prompt、模型提供方契约或 AI 数据流，通常需要同时检查 `AIService` 与 `backend`。

### `deploy`
- 负责部署描述、打包支持和环境交付相关文件。
- 运行配置或基础设施相关调整，可能需要同步更新这里的内容。

### `docs`
- 负责项目文档。
- 接口、配置、部署方式或使用行为发生明显变化时，应同步更新文档。

### `AIlog`
- 仅用于调试日志。
- 不要在这里放源码、长期配置或业务流程逻辑。

## 跨模块联动判断
- 修改后端 API 契约时，检查 `frontend` 是否需要同步调整请求或响应处理。
- 修改 AI 交互行为时，检查 `backend`、`AIService` 和 `docs` 是否需要一起更新。
- 修改运行配置时，检查 `backend`、`deploy` 和相关文档是否需要同时变更。
- 修改上传、生成文件或资源路径行为时，检查 `uploads`、`backend` 和部署配置是否仍然一致。

## 后端约定
- Controller 保持薄层，业务逻辑放在 service。
- 沿用现有的统一响应和异常处理方式。
- 数据库结构变更时新增 Flyway migration，不修改已在其他环境执行过的历史 migration。
- entity、repository、service、controller、test 的改动要保持一致。
- 配置优先走环境变量或 profile 配置，不要硬编码密钥。

## 文档约定
- 当出现以下类型变更时，更新 `docs`：
  - 功能新增或功能行为调整
  - 技术结构变更
  - 部署方案变更
  - 数据库结构变更
- 不要因为纯代码清理、死代码删除、内部重构但无外部行为变化、小型兼容性修复而单独新增文档。
- 文档内容应与当前实际代码保持一致。

## 注意事项
- `target`、构建产物、生成文件、日志和运行时目录都不是事实来源。
- 遇到看起来像生成代码、镜像文件或跨模块复制内容时，先读清楚再改。
- 与当前任务无关的脏工作区改动不要回退，除非它直接阻碍本次任务。

## 默认工作流程
1. 先判断需求归属哪个模块。
2. 再判断是否有相邻模块依赖这项行为。
3. 用最小但完整的改动集同时覆盖代码、配置、测试和文档。
4. 结束前核对对应模块的既有约定是否被保持。
